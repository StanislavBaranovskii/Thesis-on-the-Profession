version: '3.7'

services:
  kibana:
    container_name: kibana
    image: bitnami/kibana:7.17.9
    volumes:
      - ~/Thesis-on-the-Profession/logs/kibana.yml:/usr/share/kibana/config/kibana.yml
    ports:
      - 5601:5601
